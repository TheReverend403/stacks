[project]
name = "stacks"
version = "0.1.0"
description = "Some docker-compose stacks 🤷"
requires-python = ">=3.14,<3.15"
dependencies = []

[dependency-groups]
dev = [
    "prek>=0.4.5",
    "yamllint>=1.38.0",
]
